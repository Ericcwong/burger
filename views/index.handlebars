    <div class="header">
        <img id="burgerImage" src="assets/images/burger.png" alt="Cartoon burger">
    </div>
        <div class="leftContainer">
        <ul>
            {{#each}}
        </ul>
    </div>
    <div class="container">
        <h2>Burger Devourer!</h2>

        <form action="/" method="POST">
            <textarea name="burger_name"cols="50" rows="10" type="text"></textarea><br>

            <button class="submitButton" type="submit">Order it!</button>
        </form>
        <h4>Enter a burger you want to eat!</h4>
    </div>